const light = {
  darkMode: false,
};

const dark = {
  darkMode: true,
};

const defaultTheme = {
  darkMode: false,
};

export const lightThemee = { ...defaultTheme, ...light };
export const darkThemee = { ...defaultTheme, ...dark };
